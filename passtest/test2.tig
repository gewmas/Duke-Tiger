/* arr1 is valid since expression 0 is int = myint */
let
	var N := 8

	type intArray = array of int
	var col := intArray [ N ] of 0
in
	for r := 0 to N-1
		do (col[r] := r; print("for\n"))
	;
	col[0]+col[1]+col[2]+col[3]+col[4]+col[5]+col[6]+col[7]
end
