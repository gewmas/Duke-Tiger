/* Seems to have problem access buffer in isdigit*/


let 

 type any = {any : int}
 var buffer := "12"

function readint(any: any) : int =
 let var i := 0
     function isdigit(s : string) : int = 
		  ord(buffer)>=ord("0") & ord(buffer)<=ord("9")
     function skipto() =
       while buffer=" " | buffer="\n"
         do buffer := "12"
  in skipto();
     any.any := isdigit(buffer);
     while isdigit(buffer)
       do (i := i*10+ord(buffer)-ord("0"); buffer := "12");
     i
 end

 var any := any{any=0}
var i := readint(any)

 type list = {first: int, rest: list}

 


  /* BODY OF MAIN PROGRAM */
 in i
end

